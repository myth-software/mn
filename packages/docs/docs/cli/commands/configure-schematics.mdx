# configure-schematics

configures schematics to apply in configuration

**dependencies**

none

**arguments**

| name              | long           | short | prompt                                   | enum                |
| ----------------- | -------------- | ----- | ---------------------------------------- | ------------------- |
| schematics        | --schematics   | -s    | schematics to scheme                     | all schematics      |
| excludes from all | --excludes-all | -e    | databases to exclude from all schematics | databases in schema |

**cli example**

```sh title="terminal"
npx mn configure-schematics -s mirage -e logs
```

**prompt example**

```sh title="terminal"
npx mn configure-schematics
```

```sh
schema schematic is required, schemed automatially

schematics to scheme:
[ ] controllers
[ ] interfaces
[x] mirage
[ ] react-query
[ ] rtk-query
[x] fixtures

databases to exclude from all schematics:
[x] logs
[ ] sets
[ ] overlays

databases to exclude from mirage:
[ ] overlays
[x] sets

databases to exclude from fixtures:
[x] overlays
[x] sets
```
